# Fetching Data from an API

### What I learned today
- How to fetch data from a REST API using `requests`
- How API pagination works
- Converting JSON responses into pandas DataFrames
- Selecting useful fields from API data
- Why `DataFrame.append()` no longer works in pandas 2.x
- Using `pd.concat()` to combine data efficiently
